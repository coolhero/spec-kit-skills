# Interface: cli

> Command-line tools, shell commands, terminal utilities.

---

## Signal Keywords

### Semantic (S0 — for init inference)

**Primary**: CLI, command line, terminal tool, shell script, flags, arguments, Commander, yargs, clap, Click, Cobra, subcommands

**Secondary**: interactive prompt, progress bar, piping, stdin, stdout, REPL

### Code Patterns (R1 — for source analysis)

- Binary entry points: `bin/` directory, `package.json` `bin` field
- CLI framework imports: Commander, yargs, clap, cobra, click, argparse
- `main()` with argument parsing patterns

---

## Module Metadata

- **Axis**: Interface
- **Common pairings**: —
- **Profiles**: cli-tool
