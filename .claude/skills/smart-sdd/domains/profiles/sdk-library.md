# Profile: sdk-library

> Library, SDK, or framework distributed as a package for other developers to consume. Extension-point architecture with public API stability requirements.

interfaces: [cli]
concerns: [plugin-system]
archetype: sdk-framework
