# Profile: cache-service

> In-memory cache server with connection pooling, eviction policies, and graceful shutdown support.

interfaces: [cli]
concerns: [graceful-lifecycle, connection-pool]
archetype: cache-server
