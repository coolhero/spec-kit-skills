# Concern: auth (reverse-spec)

> Authentication detection. Identifies auth implementation patterns.

## R1. Detection Signals

> See [`shared/domains/concerns/auth.md`](../../../shared/domains/concerns/auth.md) § Code Patterns
