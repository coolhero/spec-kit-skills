# Spec Draft — [FID]-[Feature Name]

> **Generated by**: reverse-spec Phase 4 from source analysis + runtime exploration.
> **Purpose**: Seeds `speckit-specify` so it refines rather than creates from scratch.
> **Artifact Separation**: This file contains REQUIREMENTS ONLY — no source code references.
> Source analysis details are in `pre-context.md`. Traceability via `[source: B###]` tags.

---

## Feature Description

[2-3 sentence description of what this Feature does from the user's perspective.
NOT what the code does — what the USER experiences.]

---

## Functional Requirements

> Each FR derived from: SBI behaviors (P1/P2), UI Flow Spec steps, Component Tree analysis.
> UI control types (Dropdown, Slider, etc.) are explicit — NOT abstracted to generic "input."

### [Group 1: Primary User Flow]

- **FR-001**: [Requirement with specific UI control types and interaction details]
  - UI: [exact control — e.g., "Dropdown showing only configured providers' models"]
  - Trigger: [how user initiates — e.g., "Click [+ Create KB] button in top-right of KB list page"]
  - Result: [observable outcome — e.g., "Creation dialog opens with Name, Model, Dimensions fields"]

- **FR-002**: [Next step in the flow]
  - UI: [control type]
  - Trigger: [action]
  - Result: [outcome]

### [Group 2: Secondary Flow]

- **FR-00N**: ...

### [Group N: Error Handling / Edge Cases]

- **FR-E01**: [Error handling requirement]
  - Trigger: [error condition — e.g., "API key not configured"]
  - UI Feedback: [what user sees — e.g., "Actionable error: 'Go to Settings > Provider to add your key'"]

---

## Success Criteria

> Each SC is TESTABLE — includes specific user action, expected outcome, and verify method.
> SC descriptions use interaction verbs (click, select, drag, type), not state verbs (exists, shows, has).

### Happy Path

- **SC-001**: [Action] → [Observable Result] → [Verify Method]
  - Example: "User clicks [+ Create KB] → fills name 'Test' → selects model → clicks Create → KB 'Test' appears in list with 0 documents"

- **SC-002**: [Data mapping accuracy SC — for clickable references]
  - Example: "User clicks citation [2] → tooltip shows source #2's file name and content preview (NOT source #1 or #3)"

### Error Paths

- **SC-E01**: [Error trigger] → [Expected UI feedback]
  - Example: "User clicks Create with empty name → red border on name input, Create button stays disabled"

- **SC-E02**: [External dependency error]
  - Example: "API key not configured → toast: 'Configure OpenAI key in Settings > Provider' with link to settings"

### Cross-Feature

- **SC-X01**: [Cross-feature integration SC]
  - Example: "User attaches KB to assistant → sends message → response contains citation badges → badges link to correct KB sources"

---

## Data Model (Draft)

> Entity names and key fields observed from source analysis.
> Full schema will be defined in plan.md data-model.md.

| Entity | Key Fields | Relationships |
|--------|-----------|---------------|
| [Entity1] | [field1, field2, ...] | [has many X, belongs to Y] |

---

## UI Flow Specifications (Reference)

> Copied from pre-context.md § UI Flow Specifications for speckit-specify's reference.
> These flows are the PRIMARY source for the FR/SC above.

### Flow: [Flow Name 1]

| Step | User Action | UI Control | Response | State Change |
|------|------------|------------|----------|-------------|
| 1 | [action] | [control] | [response] | [state] |

Error paths:
- [trigger] → [feedback]

---

## Conversion Traceability

> Maps each FR/SC back to its source: SBI behavior (B###), UI Flow Spec step, or edge case.

| FR/SC | Source | Evidence |
|-------|--------|---------|
| FR-001 | UI Flow: [Flow1] Step 1 | Runtime observation: button at top-right |
| FR-002 | UI Flow: [Flow1] Step 2-4 | Runtime observation: dropdown with auto-fill |
| SC-001 | UI Flow: [Flow1] full path | Runtime: created KB appeared in list |
| SC-E01 | UI Flow: [Flow1] error path | Runtime: red border observed on empty name |
| FR-E01 | SBI: B105 | Code: error handler in KnowledgeService.ts |
